# Railway Configuration
# This file configures Railway deployment for the monorepo

[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm build"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
