'use client'

/**
 * Empty Field Component
 * 
 * A field component that renders nothing.
 * Useful for UI fields that only display descriptions without any input.
 */
const EmptyField = () => {
  return null
}

export default EmptyField

